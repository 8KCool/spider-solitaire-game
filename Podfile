# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

target 'Spider Solitaire' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Spider Solitaire
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'

  target 'Spider SolitaireTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Spider SolitaireUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
